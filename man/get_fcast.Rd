% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_fcast.R
\name{get_fcast}
\alias{get_fcast}
\title{Rolling origin epidemic forecasts and model evaluation}
\usage{
get_fcast(df, eval_start_date, h = 4, top_n = 3, extra_models = NULL)
}
\arguments{
\item{df}{Data frame of weekly observations containing
\code{target_end_date} (Date) and \code{observation} (numeric).}

\item{eval_start_date}{Date or string coercible to Date. First date at
which forecasts are evaluated. At least 52 weeks of data must precede
this date.}

\item{h}{Integer. Forecast horizon in weeks. Default is 4.}

\item{top_n}{Integer. Number of top ranked models included in the
ensemble. Default is 3.}

\item{extra_models}{Named list of additional forecasting models passed to
\code{fabletools::model()}. Each element must reference the \code{observation}
column and be compatible with \code{fabletools::forecast()}.

Custom models can be constructed using the fable modelling framework,
see the \href{https://fabletools.tidyverts.org/articles/extension_models.html}{fabletools extension_models vignette}.

The name of each list element is used as the model label in the output.}
}
\value{
An object of class \code{accida_cast} containing:
\describe{
\item{forecast}{Final \code{h} week ahead forecasts for all models and the ensemble.}
\item{score}{Model ranking based on rolling origin CRPS.}
\item{plot}{ggplot object showing forecasts and prediction intervals.}
}
}
\description{
Fits multiple time series models to weekly incidence data and evaluates
short term predictive performance using an expanding window rolling
origin scheme.
}
\details{
From \code{eval_start_date} onwards, models are repeatedly refitted on all data
available up to each evaluation time point and used to forecast the next
\code{h} weeks. Forecasts are compared to observations using the Continuous
Ranked Probability Score (CRPS).

Models are ranked by mean CRPS across evaluation periods. The best
performing \code{top_n} models are combined into an equal weight ensemble.
A final \code{h} week ahead forecast is then produced by refitting selected
models using the full dataset.
}
\examples{
\dontrun{
extra_models <- list(
  CUSTOM_ARIMA = ARIMA(observation ~ pdq(1, 1, 0)),
  PROPHET = fable.prophet::PROPHET(observation)
)

get_fcast(
  df,
  eval_start_date = "2025-01-01",
  h = 4,
  top_n = 3,
  extra_models = extra_models
)
}

}
